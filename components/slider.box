let slider = {
    data:{
        image:"images/5.jpg",
    },
    start:function(){
        let bm = this;
        setInterval(function(){
            let num = Math.floor((Math.random() * 5) + 1);
            bm.image = "images/"+num+".jpg";
        },5000)
    }
}